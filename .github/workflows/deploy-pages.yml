name: Validate Docs
on:
  push:
    branches: ["main"]
    paths:
      - "docs/**"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check index.html exists
        run: test -f docs/index.html && echo "docs/index.html OK"
